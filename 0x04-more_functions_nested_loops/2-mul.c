#include "main.h"

/**
 *mul - A function that multiplies two integers
 *@a: Stores value to be multiplied
 *@b: Stores value to be multiplied
 *Return: Always 0 (Success)
 */

int mul(int a, int b)
{
int times = a * b;
return (times);
}
