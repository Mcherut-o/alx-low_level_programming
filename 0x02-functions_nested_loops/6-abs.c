#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer
 * @b: Checks for absolute values to be printed
 *Return: Always 0 (Success)
 */

int _abs(int b)
{
if (b < 0)
{
b = -b;
}
return (b);
}
